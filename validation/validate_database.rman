# Validate database files without producing a backupset
VALIDATE DATABASE;

# Check for physical corruption recorded by RMAN
LIST FAILURE;

# Validate recoverability from existing backups
RESTORE DATABASE VALIDATE;
